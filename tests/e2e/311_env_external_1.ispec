---
environment:
  OUTSIDE_PAGER1: a${PAGER}a
  OUTSIDE_PAGER2: b$PAGER
---
[@local]$ echo $OUTSIDE_PAGER1
alessa
[@local]$ echo $OUTSIDE_PAGER2
bless
# from shellinspector.yaml
[@local]$ echo $OUTSIDE_PAGER3
cless
